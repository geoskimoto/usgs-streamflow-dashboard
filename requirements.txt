# USGS Streamflow Dashboard Dependencies
# For Render deployment

# Web Framework
# dash pinned: it bundles its own plotly.js client-side at package build time
# (independent of the separately pip-installed `plotly` package below), and
# 4.4.1 changed usgs_dashboard/components/map_component.py's go.Scattermap
# rendering enough to break the map. 4.0.0 is the last version verified working.
dash==4.0.0
dash-bootstrap-components>=1.5.0
gunicorn>=21.2.0
streamflows-auth @ file:///home/geoskimoto/projects/streamflows-auth
PyJWT>=2.9.0

# Visualization
# Pinned to match the last verified-working venv exactly (see dash note
# above) after a clean rebuild resolved the unpinned >= to 7.0.0. Python-side
# figure construction was confirmed unaffected either way; kept pinned as a
# known-good baseline rather than re-introducing an untested version.
plotly==6.5.2
matplotlib>=3.5.0
kaleido>=1.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# DataOps API Client
requests>=2.28.0
python-dotenv>=0.19.0

# Direct PostgreSQL access (used when USE_DATAOPS_API=false)
psycopg2-binary>=2.9.0

# ResidCast direct-DB mode (RESID_CAST_USE_API=false) — resid_cast_db_client.py
sqlalchemy==2.0.49

# USGS Data (fallback for streamflow_analyzer.py)
dataretrieval>=1.0.10

# Geospatial (optional but recommended)
geopandas>=0.13.0

# Statistical analysis
scipy>=1.8.0

# Performance & Caching
diskcache>=5.6.0
pyarrow>=14.0.0

# Additional utilities
python-dateutil>=2.8.0

# Excel export support
openpyxl>=3.0.0

